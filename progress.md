Original prompt: 做新项目了

Source documents: `C:/Users/5/Downloads/冰汽时代小量版_游戏策划_v0.1.md` and `C:/Users/5/Desktop/UI整体方向.txt`.

Goal: Build a playable P0 Web prototype with an original name and visual assets, 20-day survival loop, fixed 6/8/10 ring slots, resource and worker decisions, research unlocks, storm, and outcome. The referenced document's P1-P4 features are later stages.

Completed: Original project name `余烬城`; standalone browser app with central generator and fixed ring map; building, staffing, upgrade, demolition, research, law, event, heat, illness, storage, autosave, storm, settlement, and local ranking. Rule engine is separate from DOM rendering.

Verification: `npm test` covers a legal 20-day win with both ring unlocks and a fuel-outage loss. Playwright desktop game client and 375×812 mobile interaction flow checked intro, building, staffing, law, research, Day 3 report, save reload, ranking, and restart. Screenshots are in ignored `output/web-game/`.

Next stage: Expand event and law content, add named residents, weather/audio polish, and tune balance with playtest data. Cloud rankings and MCP Agent play remain later project phases.

Resource HUD update: Added the four 30×30 PNG icons supplied in `资源素材.zip` to `assets/resources/`. Each resource now displays its icon on the left and its name above the value on the right. Verified all four images load and the layout remains readable at 375×812 and 320×568.

City background update: Copied the supplied 405×720 city-wall artwork to `assets/city-wall.jpg` and aligned the transparent city map over the walled interior. Moved outer slots inward slightly so their tap targets stay inside the wall. Checked 375×812, 320×568, and desktop screenshots; engine and mobile interaction checks pass.

Day/night background update: Replaced the previous wall JPG with the supplied matching 669×1300 day/night PNGs in `assets/city-day.png` and `assets/city-night.png`. City view switches at 06:00/18:00 using game time; night image preloads. Verified alignment, mobile/desktop layouts, night save/reload, and existing gameplay tests.

v0.2 social crisis update: Removed the city-map caption requested by the user. Added low-hope departure stages, bounded daily flight with carried supplies, a 24-hour despair deadline and possible city dissolution; added protest, production penalties, a 48-hour riot ultimatum, aftermath, and route-dependent ruler outcomes. Both crises can coexist with prioritized event reports. The City panel provides daily relief and concession actions; result and score track departures and riot history. Old v1 saves gain default social fields on load. Verification: six engine tests cover the full 20-day run, outage loss, flight, simultaneous crisis recovery, riot timeout, and city dissolution; mobile browser flow checks UI actions, save/reload, and narrow-screen crisis alerts.

Slot layout update: Kept the 6/8/10 ring counts, ring 1 angles, ring 2 offset 22.5°, ring 3 offset 18°, and generator dimensions. Reduced 44px slots to 37.4px and compact 40px slots to 34px; slightly thinned the slot rim. Balanced ring 2/3 center radii at 34.5%/48% of map width and matched their guide rings so adjacent rings do not overlap. Browser geometry checks at 320×568, 375×667/812, and 430×720/900 show positive gaps between ring 1/2 and ring 2/3; gameplay and mobile UI flows still pass.

Snow atmosphere update: Added a low-density, transform-animated CSS snow layer inside the city background, behind the map and controls. Day/night color and opacity differ slightly; Day 20 adds a second layer and faster fall. Pointer events stay disabled, reduced-motion users get static snow, and render resets preserve animation phase. Browser checks cover day/night/storm styles, animation movement, click-through, rerender continuity, and reduced motion; existing mobile UI flow and engine checks remain the regression gate.

Ruler identity update: New runs now follow naming → opening copy → play. The name field is labeled 执政者姓名, validates 2–12 Unicode code points, and remembers the last confirmed name for restart. A browser-local UUID is generated once and passed to the shared game action as playerId; new game records, result copy, rankings, and render_game_to_text carry both identity fields. Existing active saves gain the browser ID and last name or a legacy label without losing progress; pre-name intro saves return to naming. Browser checks cover validation, autofill, stable ID, rename on restart, leaderboard display, legacy save migration, and 320px layout. Engine suite now has seven passing checks. Future Telegram Mini App, mini-program, and AI Agent adapters can supply the same internal playerId field; no platform SDK was added.

Audio / settings / MCP stage: Added `audio.js` (Web Audio) — three BGMs (炉火未熄 / 逼近寒潮 / 白灾将至, copied to `assets/bgm/`) switch by game state with sequential crossfades (hearth days 1–7, cold-front days 8–16, white-fall from day 17 or riot/despair crises; fades out at settlement), eight SFX wired to UI clicks, build/law/event/furnace transitions, and three independent persisted volumes (master/BGM/SFX). Official logo (assets/logo/) shown on the new loading splash and the naming page. New top-right ⚙ settings panel pauses game time while open and offers the three volume sliders, a snow-particle toggle, a double-confirmed restart, and a back button. `platform.js` prepares Telegram Mini App support (Telegram.WebApp ready/expand, playerId/displayName mapping) without touching game code. `mcp-server.mjs` is a zero-dependency stdio MCP server exposing 15 tools (create_ruler, get_game_state, build, assign_workers, sign_law, choose_event_option, advance_time, get_result, …) that all route through the same act()/advanceHours() engine as human play; get_result writes a leaderboard record in the same schema with no AI flag. Verification: engine suite now has nine passing checks (two new MCP tests cover shared-rule validation and single-record settlement); browser smoke test confirmed splash/naming logo, settings pause, volume persistence, snow toggle, restart confirm, full audio prefetch after first gesture, and zero console errors. Assets duplicated from the gitignored Emberfall_City__* delivery folders; repo published to github.com/riki-aowu/emberfall-city-twenty-days-below for GitHub Pages.

Header / repository move stage: Moved the settings and heatmap controls into a borderless vertical icon stack beneath Hope; kept access to settings on naming and intro panels. Removed the header border causing the dark seam over the city image. Fixed the BGM URL passed to fetchBuffer and the building-complete SFX comparison; these were revealed by the updated browser smoke test's 404 checks. Verified at 320×568, 375×812, and 430×720 with click-through checks, all nine engine/MCP tests, and a full mobile flow with no failed requests. Destination repository: github.com/Qisi3344/Emberfall_City_Qisi.

2026-09-22 difficulty stage: Synced current main before work. Added mild/extreme mode choice after the opening and a once-per-new-run tutorial prompt; legacy saves default to mild without a new prompt. Extreme uses shared difficulty configuration for weather, opening stocks, production, coal, illness, social pressure, repeatable problem events, refugees, law costs, final victory and score. Dynamic UI shows effective event/law values, mode appears in results and rankings, and MCP can choose difficulty while reusing the same engine. All 37 engine/MCP checks pass repeatedly; mobile browser checks cover 375px extreme and 320px mild opening, tutorial, settings, resume, event values and legacy saves. Pending: real-player balance feedback on large/small city strategies; Telegram BotFather deployment remains outside this stage.
